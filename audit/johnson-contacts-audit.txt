
── Mental-attribute column order ─────────────────────────────────────────────
The book prints `B Q S I W C`. SR3's character sheet orders them `B Q S C I W`.
If the entry was made against the sheet's order, the book's I/W/C values land in
charisma/intelligence/willpower — a rotation, not a scatter of typos.

  match ONLY under the rotation:            0
  match exactly (book order):               54
  all three equal, so undecidable:          6
  match neither — look at these by hand:    0

Book stat blocks parsed: 61
Generator entries:       62
Matching:                51
With differences:        9

⚠ Book blocks with no generator entry (1) — a heading the name-finder could not read, or a genuinely missing contact:
  CORPORATE SECURITY  (book line 57)

── Differences ──────────────────────────────────────────────────────────────
⚠ NOT all of these are errors. The generator deliberately uses augmented values,
  consolidates cyberware to hit the printed Essence, and leaves Reaction to the
  system except where the book names a known booster. Read each one.

Club Owner (p.42)
    body: generator 8, book 9 — matches the NATURAL value 8, so this is the parenthetical convention, not an error
Freedom Fighter (p.46)
    strength: generator 5, book 7 — matches the NATURAL value 5, so this is the parenthetical convention, not an error
Mr. Fix-It (p.47)
    body: generator 10, book 12 — matches the NATURAL value 10, so this is the parenthetical convention, not an error
    quickness: generator 5, book 7 — matches the NATURAL value 5, so this is the parenthetical convention, not an error
    strength: generator 9, book 11 — matches the NATURAL value 9, so this is the parenthetical convention, not an error
Corp Bodyguard (p.49)
    quickness: generator 7, book 8 — matches the NATURAL value 7, so this is the parenthetical convention, not an error
    strength: generator 6, book 7 — matches the NATURAL value 6, so this is the parenthetical convention, not an error
Ambitious Mafia Lieutenant (p.53)
    quickness: generator 4, book 5 — matches the NATURAL value 4, so this is the parenthetical convention, not an error
    strength: generator 5, book 6 — matches the NATURAL value 5, so this is the parenthetical convention, not an error
Triad Member (p.54)
    quickness: generator 6, book 7 — matches the NATURAL value 6, so this is the parenthetical convention, not an error
Troll Street Dealer (p.57)
    body: generator 8, book 9 — matches the NATURAL value 8, so this is the parenthetical convention, not an error
    quickness: generator 4, book 6 — matches the NATURAL value 4, so this is the parenthetical convention, not an error
    strength: generator 9, book 11 — matches the NATURAL value 9, so this is the parenthetical convention, not an error
Highway Patrol (p.62)
    body: generator 5, book 7 — matches the NATURAL value 5, so this is the parenthetical convention, not an error
Dock Worker (p.67)
    body: generator 10, book 11 — matches the NATURAL value 10, so this is the parenthetical convention, not an error
